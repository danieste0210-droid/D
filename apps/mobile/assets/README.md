# Assets pendientes

Faltan los archivos binarios referenciados en `app.json` (`icon.png`, splash, adaptive icon Android). Agregar antes de compilar con EAS Build:

- `icon.png` (1024x1024)
- `splash.png`
- `adaptive-icon.png` (Android, foreground)

Usar el morado de marca `#AB47BC` como fondo, igual que la versión original.
