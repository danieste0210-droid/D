# Próximos pasos

Backend y mobile ya tienen toda la lógica de negocio central implementada y probada contra una base de datos real (no solo compilación). Orden sugerido para lo que queda:

## 1. Backend — base transaccional

- [x] `npm install` en la raíz y `nest build` compilan limpio (`strictPropertyInitialization` desactivado en `apps/api/tsconfig.json`, patrón estándar para DTOs de Nest/class-validator)
- [x] **Base de datos real conectada**: proyecto Postgres "daniel-appk" en Neon (vía Vercel Storage), `prisma migrate dev` corrido con éxito. `schema.prisma` usa `DATABASE_URL` (pooled, runtime) + `DIRECT_URL` (directa, solo para migraciones — el pooler de Neon no soporta los locks de `migrate`). Ambas están en `apps/api/.env` (no versionado)
- [x] `npm run prisma:seed --workspace=apps/api` corrido contra Neon — usuarios `superadmin`/`vendedor1` (password `changeme123`), lotería demo con cierres permisivos (23:30 todos los días, para no bloquear pruebas manuales)
- [x] **Smoke tests end-to-end contra la base real** (login, ventas, premios, reversión, refresh tokens, 2FA, historial de cierres, push notifications) — encontró y corrigió 3 bugs reales que el chequeo de tipos no detectaba:
  1. `@Public()` no se aplicaba: `JwtAuthGuard` nunca leía esa metadata, así que **todo** login fallaba con 401 (`apps/api/src/common/guards/jwt-auth.guard.ts`)
  2. El UUID de la lotería en el seed (`00000000-...-0001`) no era un UUID v4 válido (nibbles de versión/variante en cero) y `class-validator` lo rechazaba — corregido en `prisma/seed.ts`
  3. **Colisión de refresh tokens**: el payload del JWT no tenía identificador único, así que dos logins en el mismo segundo (mismo `iat`) generaban un token idéntico byte a byte y chocaban contra el `@unique` de `refresh_token`, tumbando el login con 500. Se agregó un `jti` (`randomUUID()`) a cada token (`auth.service.ts`)
- [x] **Auth**: rotación de refresh tokens (`POST /auth/refresh`), revocación (`POST /auth/logout`), historial de sesiones (`GET /auth/sessions`, usa `UserSession`), 2FA TOTP para admin/super (`POST /auth/2fa/setup|verify|disable`, `otplib` v12 + `qrcode`)
- [x] **`User.twoFactorSecret` cifrado en reposo**: AES-256-GCM (`src/common/crypto/secret-encryption.ts`), clave en `ENCRYPTION_KEY` (.env, 64 hex = 32 bytes). El round-trip y la detección de manipulación (auth tag) se probaron en Node puro; el flujo completo (setup → verify → login con código) se re-probó contra Neon y el valor guardado ya no coincide con el secret en texto plano
- [x] **Closures**: `isLotteryOpen(lotteryId, at)` en `closures.service.ts` (usa `Intl.DateTimeFormat` con `TIMEZONE`, no la hora del servidor) conectado en `sales.service.create()` y `cancelBySeller()`
- [x] **Lotteries**: cambios de horario (`create`/`update`/`remove` en `closures.service.ts`) quedan registrados en `LotteryScheduleChange` (before/after, quién) dentro de una transacción Prisma
- [x] **Sales**: límite de monto máximo por número (`Lottery.maxAmountPerNumber`) implementado en `sales.service.create()`
- [x] **Results/Awards**: `processAwards()` en `lotteries.service.ts` dentro de `$transaction` (crea `Result` + un `Award` por cada venta activa ganadora). **Pendiente de negocio real**: `Lottery.payoutMultiplier` (default 60x) es un placeholder — reemplazar por la tabla de pagos real antes de operar con dinero
- [x] **Reversión de premios**: `results.service.reverse()` marca el `Result` revertido y pasa los `Award` `pending` asociados a `reversed`; los que ya estaban `paid` se reportan aparte (`awardsRequiringManualReview`) para seguimiento humano, nunca se tocan solos
- [x] **Reportes**: export a Excel (`GET /reports/sales/export/excel`, `exceljs`) y PDF (`GET /reports/sales/export/pdf`, `pdfkit`)
- [x] **Notificaciones push** (`src/modules/notifications`): `POST /auth/push-token` registra el Expo push token del usuario (un solo token por `User`, no multi-dispositivo real); `processAwards()` notifica a cada vendedor con ventas ganadoras fuera de la transacción (un fallo de envío no revierte los premios ya confirmados — probado con un token con formato válido pero falso: el warning queda en el log y la venta/premio se crean igual)
- [x] **Aviso push de cierre próximo** (`ClosuresReminderService`, `@nestjs/schedule`): cron cada 5 min que barre en ventanas contiguas (aviso ~15 min antes del cierre, ventana de 5 min = mismo intervalo del cron, así cada cierre cae en exactamente una ejecución por día sin necesitar una tabla de log de notificaciones enviadas). Se extrajo la conversión de zona horaria (antes duplicada en `closures.service.ts`) a `common/time/local-time.ts`, compartida por ambos servicios. 7 tests unitarios cubren la selección de ventana, lotería inactiva/bloqueada, y notificar a todos los vendedores con token. Probado también contra la base real invocando la lógica directamente (sin esperar al reloj): identificó correctamente el cierre sembrado (lunes 23:30) como "próximo" 17 minutos antes
  - Pendiente: WhatsApp Business API para clientes finales (requiere cuenta/credenciales de un proveedor externo, fuera de mi alcance)
- [x] **Rate limiting por endpoint sensible**: `@Throttle({ default: { limit: 5, ttl: 60_000 } })` en `POST /auth/login` y `POST /auth/2fa/verify` (mitiga fuerza bruta de contraseña/código TOTP), `40/60s` en `POST /sale/process` (deja margen a un vendedor real, limita ráfagas automatizadas). Probado en vivo: 7 logins seguidos con credenciales inválidas dan 401 en los primeros 5 y 429 (Too Many Requests) en el 6to y 7mo

## 2. Mobile — flujos funcionales

- [x] Pantalla de ventas: listado (`app/(app)/sales/index.tsx`, React Query + pull-to-refresh + cancelar con motivo) y formulario de nueva venta (`app/(app)/sales/new.tsx`, selector de lotería, validación, cola offline si no hay red)
- [x] Pantalla de loterías (`app/(app)/lotteries/index.tsx`): listado + crear + bloquear, gestión visible solo para admin/super
- [x] Pantalla de cierres (`app/(app)/closures/index.tsx`): listado (visible a todos los roles, igual que `GET /closures/getAll`) + crear/eliminar solo admin/super
- [x] Pantalla de resultados (`app/(app)/results/index.tsx`): publicar resultado dispara `processAwards`, lista de resultados recientes por lotería con botón "Revertir" (solo rol `super`, coincide con el backend), lista de premios pendientes de pago
- [x] Pantalla de usuarios (`app/(app)/users/index.tsx`): crear + desactivar (soft-delete). El listado (`GET /user/super`) el backend lo restringe solo a rol `super` (no admin) — la pantalla respeta esa asimetría con un mensaje para admin en vez de romper
- [x] Pantalla de reportes (`app/(app)/reports/index.tsx`): resumen agregado (`GET /reports/sales`) + descarga de Excel/PDF vía `expo-file-system` (los endpoints devuelven binarios, no JSON) y `expo-sharing` para abrir/compartir el archivo
- [x] Tabs condicionales por rol en `app/(app)/_layout.tsx` (`href: null` para ocultar Resultados/Reportes/Usuarios según rol) — evita que un vendedor navegue a una pantalla que el backend le va a rechazar con 403
- [x] **Refresh automático de `accessToken`**: `apiFetch` (`src/api/client.ts`) reintenta una vez con `POST /auth/refresh` ante un 401, comparte una sola llamada de refresh entre requests concurrentes, y desloguea si el refresh también falla
- [x] **Cola offline** (`src/offline/sync.ts`): `useOfflineSync()` escucha reconexión con `@react-native-community/netinfo` y drena `src/offline/queue.ts` contra `POST /sale/process`; si el servidor rechaza una venta encolada (p.ej. cierre ya pasó) se descarta en vez de bloquear el resto de la cola
- [x] **Notificaciones push**: `src/notifications/registerPushToken.ts` pide permisos y registra el Expo push token contra `POST /auth/push-token` al entrar a la app autenticada
  - **No probado en dispositivo real** (requiere hardware físico; los simuladores no reciben push) — la plomería está completa y verificada del lado del backend, falta la verificación end-to-end con un dispositivo
- [x] **`npx expo install --fix`**: alineó `typescript` a `~5.3.3` (la versión que espera el SDK 51; antes se usaba `5.9.3` hoisted del monorepo). npm instaló una copia anidada en `apps/mobile/node_modules` sin tocar la versión que usa el backend. **Hallazgo real**: con la versión correcta, `tsc` sacó 10 errores `implicitly has an 'any' type` en callbacks de `.map()`/`.find()`/`.reduce()` sobre datos de React Query (`lotteries/index.tsx`, `closures/index.tsx`, `results/index.tsx`, `reports/index.tsx`, `sales/new.tsx`) que la versión más nueva de TS estaba inferiendo bien pero 5.3.3 no — corregidos con anotaciones de tipo explícitas. Sin este fix, el proyecto habría fallado al compilar de verdad con el toolchain real de Expo (p.ej. en EAS Build)
- [x] **`assets/icon.png` generado** (1024×1024, morado de marca sólido, encoder PNG a mano verificado con round-trip de decodificación — no hay librería de imágenes instalada). Es el único archivo que `app.json` referencia por ruta y por lo tanto el único que bloqueaba `expo start`/EAS Build; `splash.png`/`adaptive-icon.png` no hacían falta porque `app.json` solo declara `backgroundColor` para esos dos, sin ruta de imagen (ver `apps/mobile/assets/README.md`). **Sigue siendo un placeholder** — reemplazar por el logo real antes de publicar
- [x] **Editar lotería/cierre existente**: modal de edición reutilizado en `lotteries/index.tsx` (nombre + límite) y `closures/index.tsx` (hora de cierre), probado contra `PATCH /lotteries/edit/:id` y `PATCH /closures/update/:id` en la base real
- [x] **Impresión Bluetooth de tickets** (`src/printing/`):
  - `escpos.ts`: generador de comandos ESC/POS (texto, alineación, negrita, feed, corte) y QR impreso como imagen raster (`GS v 0`) en vez de los comandos QR nativos (que varían mucho por fabricante) — usa `qrcode/lib/core/qrcode` (el cálculo puro de la matriz, sin canvas/Buffer, para poder correr en React Native; ver shim de tipos en `src/types/qrcode-core.d.ts`)
  - `blePrinter.ts`: permisos runtime de Android, escaneo/conexión BLE, envío por bloques de 180 bytes (límite típico de MTU), encoder base64 propio (sin dependencias nativas)
  - `app/(app)/printer/settings.tsx`: pantalla de emparejamiento (accesible desde "Configurar impresora" en el Dashboard para rol vendedor) + botón de ticket de prueba
  - `app/(app)/sales/new.tsx`: imprime automáticamente al confirmar una venta si hay impresora emparejada, sin bloquear el flujo si falla (fire-and-forget)
  - **Verificado sin hardware**: la matemática del raster QR (dimensiones, bytes, presencia de píxeles negros) y el encoder base64 se probaron en Node contra la implementación estándar de `Buffer.toString('base64')` — coinciden exactamente en todos los casos borde
  - **NO verificado contra una impresora real**: los UUIDs de servicio/característica BLE (`0000ff00`/`0000ff02`) son un valor genérico común en impresoras BLE clonadas baratas, casi seguro hay que ajustarlos al modelo real (escanear con una app tipo "nRF Connect" contra la impresora física para encontrar los UUIDs correctos). Tampoco se probó que la impresora interprete bien el modo raster `GS v 0` ni que respete el corte de papel (`GS V 1`) -- son los puntos de mayor riesgo antes de usarlo con un vendedor real
  - **`react-native-ble-plx` no funciona en Expo Go** (es un módulo nativo) — probarlo requiere un development build de EAS
- [x] **`authStore.logout()` revoca la sesión en el servidor**: llama a `POST /auth/logout` con el refresh token antes de limpiar el estado local (fetch crudo, no `apiFetch`, para no bloquear el logout si falla la red) — probado contra la base real: después de logout, reusar ese refresh token en `/auth/refresh` da 401. De paso se agregó el botón "Cerrar sesión" en el Dashboard, que hasta ahora no existía en ninguna pantalla. También se extrajo `API_URL` (estaba duplicada en `client.ts` y `reports.ts`) a `src/api/config.ts`
- [ ] No se pudo levantar un preview visual en este entorno (sin simulador/dispositivo; Expo web requeriría instalar `react-native-web` y no representa bien el target real por el uso de Bluetooth/SecureStore nativos) — validado solo con `tsc --noEmit` (pasa limpio en todo `apps/mobile`)

## 3. Transversal

- [x] **Tests unitarios** (`apps/api/src/modules/**/*.spec.ts`, 13 tests, todos pasan): `closures.service.spec.ts` cubre `isLotteryOpen` (lotería inexistente/inactiva/bloqueada, sin cierre configurado, justo antes/después de la hora de cierre, y que use el día de la semana de America/Panama y no UTC); `lotteries.service.spec.ts` cubre `processAwards` (lotería inexistente, resultado duplicado, cálculo `amount × payoutMultiplier`, filtro de ventas activas, cero premios si no hay coincidencias, una sola notificación por vendedor aunque tenga varias ventas ganadoras). Se agregó config de Jest a `apps/api/package.json` (no existía) y se mockeó `notifications.service` en el spec de loterías porque `expo-server-sdk` es ESM puro y rompe la transformación de Jest si se carga de verdad
- [x] **Git inicializado** (no lo estaba) + **CI** (`.github/workflows/ci.yml`): job `backend` corre `prisma generate` + `lint` + `build` + `test`, job `mobile` corre `tsc --noEmit`. Ambos con `npm ci` desde la raíz (workspaces). **Hallazgo real**: ninguna de las dos apps tenía configuración de ESLint pese a tener el script `lint` en `package.json` — habría fallado en el primer push. Se instaló `@typescript-eslint` y se agregó `.eslintrc.js` mínimo en `apps/api` y `apps/mobile`; al correrlo apareció un import sin usar real (`Image` en `app/(auth)/login.tsx`), corregido. Se verificó que `prisma generate` y `npm test` corren limpio sin `.env` presente (simulando CI, que nunca tiene el `.env` real)
- [ ] Confirmar cumplimiento regulatorio (Junta de Control de Juegos u organismo aplicable en Panamá) antes de operar con dinero real — ver aviso legal en el README raíz
